APP_URL=http://localhost:8999
API_URL=http://localhost:8998
APP_PORT=8999
API_PORT=8998
VITE_API_URL=8998
DATA_PATH=./
# --- AWS S3 (prod) ---
# S3_ENDPOINT_URL= leave blank for AWS; set to http://garage:3900 if using docker-compose.dev.yml
S3_ENDPOINT_URL=
S3_BUCKET_NAME=your-bucket-name
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
# --- Auth & Database ---
POSTGRES_USER=stlvault
POSTGRES_PASSWORD=changeme_strong_password_here
JWT_SECRET=changeme_generate_with_openssl_rand_hex_32

# --- SMTP (required for email verification) ---
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=phoenix@travisspark.com
SMTP_PASSWORD=qbkebnvnerkdeosr 
SMTP_FROM=phoenix@travisspark.com

# Dev CORS (allows Vite dev server to talk to backend directly)
EXTRA_CORS_ORIGINS=http://localhost:5173
